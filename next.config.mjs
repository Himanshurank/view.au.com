/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["d126h31mg8tzcs.cloudfront.net", "resi.uatz.view.com.au"],
	},
};

export default nextConfig;
